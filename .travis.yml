language: go
go: 1.13
services:
  - xvfb
before_install: sudo apt-get -y install libgl1-mesa-dev xorg-dev libasound2-dev
script:
  - |
    cd /
    go get github.com/rakyll/statik
    cd -
    go generate
    go build .
    xvfb-run -a go test .
